name: Swift

on: [push]

jobs:
  build:

    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v3
      - name: cd directory
        run: cd calculator-spm-local
      - name: Build
        run: swift build
      - name: Run tests
        run: swift test
